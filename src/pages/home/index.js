import React from 'react';
import Layout from '../layout';
import UserAccount from './UserAccount';

const Home = () => {
    return (
        <Layout>
            <UserAccount />
        </Layout>
    );
};

export default Home;

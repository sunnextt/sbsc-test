import React from 'react';

const UserAccount = () => {
    return (
        <div>
            <div>The APi did not allow user to create profile</div>
        </div>
    );
};

export default UserAccount;
